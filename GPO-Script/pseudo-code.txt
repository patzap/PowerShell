
This script is to create a GPO that will remove the shutdown button on all users in lab

Prompt user to mandatory choose domain

This script will check first if it is in the correct domain
	if condition
Will prompt the user to tell them theyre in wrong domain
	if condition 
Then it will check if the GPO already exists
WIll prompt user that no action is needed

Once all conditions are met:

	create a for loop in all existing domain users

		it will create the GPO


